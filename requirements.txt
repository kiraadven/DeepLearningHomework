# NOTE: torch / torchvision are intentionally NOT listed here — this env
# has a pinned NVIDIA build (e.g. torch==2.8.0a0+...nv) that is not on PyPI.
# pytorch-fid / torch-fidelity also declare `torch>=...` and trigger the same
# ResolutionImpossible, so install them separately with --no-deps:
#
#     pip install -r requirements.txt
#     pip install --no-deps pytorch-fid torch-fidelity
#
numpy>=1.21
pillow>=9.0
tqdm>=4.62
matplotlib>=3.5
scipy>=1.7
tensorboard>=2.8
imageio>=2.19
